package tunnel;

import maths.Vector3f;

public class Piece {
	public Piece(int nodes, Vector3f rotation){
		//this was all a prank, it just has a bunch of different blocks that can be rotated to the right position
		
		
		
		
		//this is a set of pieces that have connections, each with a designated number of nodes. 
		//supleft, upright, left, right, downleft, downright
		//there are certain nodes which are used for each planet
		
	//pick a box that meets that node standard --> will have to define the number of nodes in an tunnel in this class.
	//returns this gameObject, along with information as to what sides it has nodes on
	}
}
